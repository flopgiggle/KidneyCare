<view class="container">
    <image wx:if="{{false}}" class="avatar" src="/images/avatar/1.png"></image>
    <text class="motto"></text>
    <view wx:if="{{false}}" class="journey-container" bindtap="onTap">
        <text class="journey">demo</text>
    </view>
  <view class="journey-container" bindtap="onInfolistTap">
    <text class="journey">首页信息</text>
  </view>
    <view class="journey-container" bindtap="onRigisterTap">
        <text class="journey">注册</text>
    </view>
    <view class="journey-container" bindtap="onChartTap">
        <text class="journey">数据图表</text>
    </view>
    <view class="journey-container" bindtap="onUploadReportTap">
      <text class="journey">上传报告</text>
    </view>

</view>